import { useRouter } from "vue-router";

export default function revealInBuiltinFileMgr(filePath: string) {
    // todo: implement revealInBuiltinFileMgr
    // const router = useRouter(); // this will not work because we are not in a component

    // router.push("/side_column_local_file");
}
